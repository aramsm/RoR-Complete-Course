<h1 class="text-center page-header">Welcome to <%= @user.username %>'s page</h1>
<div class="row">
  <div class="col-md-12 center">
    <%= gravatar_for @user, size: 150 %>
  </div>
</div>

<h4 align="center" style="margin-top: 5px; margin-bottom: 30px;">
	<%= @user.username %>'s articles
</h4>

<%= will_paginate(
  @user_articles,
  renderer: WillPaginate::ActionView::Bootstrap4LinkRenderer
) %>

<%= render 'articles/articles', object: @user_articles %>

<%= will_paginate(
  @user_articles,
  renderer: WillPaginate::ActionView::Bootstrap4LinkRenderer
) %>
